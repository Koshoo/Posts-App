import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  footer: {
    position: 'fixed',
    bottom: 0,
    left: 0,
    textAlign: 'center',
    width: '100%',
    marginBottom: '15px'
  }
}));
